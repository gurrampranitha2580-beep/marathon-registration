function Loader() {
    return (
        <section className="loader">
            <h2>Submitting Registration...</h2>
            <p>Please wait while we save the participant details.</p>
        </section>
    );
}

export default Loader;